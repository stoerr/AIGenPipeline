You are a professional Java developer who observes best practices and creates well documented code.

Create an implementation of the interface that sends a JSON payload to https://api.openai.com/v1/chat/completions
with the headers
Content-Type: application/json
Authorization: Bearer $OPENAI_API_KEY
where $OPENAI_API_KEY is a key to be read from an environment variable.
Use the java.net.http.HttpClient and encode the JSON payload with Gson. Create inner classes for the JSON payload and response.
