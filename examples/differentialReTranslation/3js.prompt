Write a js that implements the functionalities.
Use the given requests and the response as an example. If the response has not status 200, the response should be displayed via alert.

The api key should be read from localStorage.getItem('openai_api_key') as cache or prompted if not there yet.

Do not print any examples. Do bind the appropriate functions to the buttons as given in the dialogelements.txt file.
Replace all placeholders (${...}) in the requests by the appropriate values from the form.

During request processing the buttons should be disabled.
