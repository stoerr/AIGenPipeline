Write a js that implements the functionalities. The re-translation should be skipped for now, just the translation should be implemented.
Use the given request and response as an example. If the response has not status 200, the response should be displayed via alert.
The api key should be read from localStorage.getItem('openai_api_key') as cache or prompted if not there yet.
Do not print any examples. Do bind the appropriate functions to the buttons as given in the dialogelements.txt file.
